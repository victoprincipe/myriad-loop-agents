{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "oracle-pm",
  "skills": {
    "paths": [".opencode/skills"]
  },
  "command": {
    "publish": {
      "template": "Prepare and publish a new version: check git status, review diff, prompt for version bump (patch/minor/major), update version in package.json, commit with a conventional commit message, create an annotated git tag, and push commit and tag to origin",
      "description": "Bump version, commit, tag, and push a release",
      "subtask": false
    }
  }
}
